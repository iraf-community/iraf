# APSELECT task

$checkout libpkg.a ".."
$update libpkg.a
$checkin libpkg.a ".."
$exit

libpkg.a:
	apgetop.x	"../lib/apkeysdef.h"	<evexpr.h>
	apgnfn.x	"../lib/apkeysdef.h"	<syserr.h>	<ctype.h>
	aphdrs.x	"../lib/apkeysdef.h"
	apkeywords.x	"../lib/apkeysdef.h"
	apselect.x	"../lib/apkeysdef.h"	<fset.h>	<error.h>
	t_apselect.x
	;
