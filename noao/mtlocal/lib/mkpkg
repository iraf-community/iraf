# These routines are used by more than one task in the dataio package:

$checkout libpkg.a ../
$update   libpkg.a
$checkin  libpkg.a ../
$exit

libpkg.a:
	cyboow.x
	ranges.x
	getdatatype.x
	addcards.x
	;
