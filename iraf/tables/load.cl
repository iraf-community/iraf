# {LOAD.CL - LOAD SOME OF THE TABLES TASKS

# Load some of the package so the user can access them without loading.
# Avoid printing menu, but do not change the
# default value of the menus switch.
      fitsio
      tbplot
      ttools
keep
