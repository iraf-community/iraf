#
#   VOTable Tests -- These tests exercise the use of local and remote VOTables
#   by tasks in the system.

      votable_001 -- Print VOTable information
      votable_002 -- Print VOTable column information
      votable_003 -- Get a value from a VOTable cell
      votable_004 -- Copy Columns of a table
      votable_005 -- Operation on a table column
      votable_006 -- Copy a VOTable, compare output

