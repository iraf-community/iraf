#!/bin/csh

source ./build.setup

set path = ( $iraf/bin_directory `echo $path | sed 's?/usr/local/bin??' ` )

source $iraf/unix/hlib/irafuser.csh

cd x11iraf

sh clean 

sh go

cp bin/* $iraf/bin_directory/.

