# Update the tproject application code in the ttools package library
# Author: B.Simon, 25-NOV-1987

$checkout libpkg.a ../
$update   libpkg.a
$checkin  libpkg.a ../
$exit

libpkg.a:
        nextuniq.x              <tbset.h>
 	tproject.x     		<tbset.h>
	wproject.x     		<tbset.h> 
	;
