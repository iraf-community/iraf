# Make the system vendor libs.

$call update
$echo "-------------- (done) ----------------"
$exit


update:	
	$echo "-------------- CFITSIO ----------------"
	$echo "------------ (not used) ---------------"
	#@cfitsio

	$echo "-------------- X11IRAF ----------------"
	;

